import 'package:flutter/material.dart';

class AdminShiftOrders extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("Admin Shift Orders"),
      ),
    );
  }
}
